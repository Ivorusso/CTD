// Suma //

function sumar(x, y) {
    return x + y;
}

const res1 = sumar(4, 4)
console.log(res1)

// Resta //
function restar(x, y) {
    return x - y;
}
const res2 = restar(4, 4)
console.log(res2)

// Multiplicar //
function Multiplicar(x, y) {
    return x * y;
}
const res3 = Multiplicar(4, 4)
console.log(res3)

// Dividir //
function Dividir(x, y) {
    return x / y;
}
const res4 = Dividir(4, 4)
console.log(res4)

// console.log(“-------------- Testeo de Operaciones / Calculadora --------------”)


// Dividir 0 //
function DividirCero(x, y) {
    return x / y;
}
const res5 = DividirCero(4, 0)
console.log(res5)



// PARTE 3 //
// // Multiplicar //
// function Multiplicar(x, y) {
//     return x * y;
// }

// CUADRADO DE UN NUMERO //
function CuadradoDeUnNumero (c) {
    return Multiplicar(c, c);
}
const res6 = Multiplicar(9, 9)
console.log(res6);

// POTENCIA //
function Potencia() {
    
}
